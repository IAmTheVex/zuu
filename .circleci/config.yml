version: 2
jobs:
  build:

    docker:
      - image: circleci/node:8
    steps:
      - checkout
      - run: sudo npm install --global mocha
      - run: sudo npm install --global tsc
      - run: sudo npm install --global lerna
      - run: lerna bootstrap
      - run: lerna run compile