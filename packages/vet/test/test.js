'use strict';
var expect = require('chai').expect;
var index = require('../build/index.js');

describe('Commmon test', () => {
    it('should work', () => {
        expect('').to.equal('');
    });
});