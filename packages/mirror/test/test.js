'use strict';
var expect = require('chai').expect;
var index = require('../build/index.js');

describe('Mirror test', () => {
    it('should work', () => {
        expect('').to.equal('');
    });
});